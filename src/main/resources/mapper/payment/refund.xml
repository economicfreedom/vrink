<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.green.vrink.refund.repository.RefundRepository">
    <insert id="saveRefund">
        INSERT INTO refund_reason(payment_id, payment_state_id, reason, reason_detail)
            VALUE (#{paymentId},#{paymentStateId},#{reason},#{reasonDetail})
    </insert>
</mapper>